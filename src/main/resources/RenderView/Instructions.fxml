<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.image.ImageView?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.layout.AnchorPane?>

<AnchorPane fx:controller="GameManager.InstructionsController"
            xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            prefWidth="800" prefHeight="600"
            style="-fx-background-color: black;">

    <!-- background -->
    <ImageView fx:id="storyImage"
               fitWidth="800" fitHeight="600"
               preserveRatio="false"
               pickOnBounds="true"/>

    <!-- Nút Previous -->
    <Button fx:id="previousButton"
            text="⬅ Trước"
            layoutX="50" layoutY="530"
            prefWidth="120" prefHeight="45"
            style="-fx-font-size: 16px;
               -fx-text-fill: white;
               -fx-background-radius: 20;
               -fx-background-insets: 0;
               -fx-background-color: linear-gradient(to bottom right, #ffb74d, #ffe082);
               -fx-font-weight: bold;"
            onAction="#handlePrevious"/>

    <!-- Nút Menu -->
    <Button fx:id="menuButton"
            text="🏠 Menu"
            layoutX="340" layoutY="530"
            prefWidth="120" prefHeight="45"
            style="-fx-font-size: 16px;
               -fx-text-fill: white;
               -fx-background-radius: 20;
               -fx-background-insets: 0;
               -fx-background-color: linear-gradient(to bottom right, #64b5f6, #90caf9);
               -fx-font-weight: bold;"
            onAction="#handleMenu"/>

    <!-- Nút Next -->
    <Button fx:id="nextButton"
            text="➡ Tiếp tục"
            layoutX="630" layoutY="530"
            prefWidth="120" prefHeight="45"
            style="-fx-font-size: 16px;
               -fx-text-fill: white;
               -fx-background-radius: 20;
               -fx-background-insets: 0;
               -fx-background-color: linear-gradient(to bottom right, #ffd54f, #fff59d);
               -fx-font-weight: bold;"
            onAction="#handleNext"/>

</AnchorPane>
